services:
  backend:
    command: make dev-go
    watch:
      - .env
      - .env.local
      - Makefile
      - go.mod
      - go.sum
      - main.go
      - src/backend
    exclude: []
  frontend:
    command: make dev-npm
    watch:
      - Makefile
      - package.json
      - package-lock.json
      - tsconfig.json
      - src/frontend
    exclude: []
  storybook:
    command: npm run storybook
    watch:
      - .storybook
    exclude: []
