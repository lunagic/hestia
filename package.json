{
	"dependencies": {
		"lineicons": "^1.3.2",
		"preact": "^10.27.0",
		"preact-iso": "^2.11.0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.0",
		"@preact/preset-vite": "^2.10.2",
		"@storybook/preact-vite": "^9.1.10",
		"@types/node": "^24.3.0",
		"sass": "^1.93.2",
		"storybook": "^9.1.10",
		"typescript": "~5.8.3",
		"vite": "^7.1.2"
	},
	"private": "true",
	"scripts": {
		"build": "tsc -b && vite build src/frontend",
		"fix": "npx @biomejs/biome check --write",
		"lint": "npx @biomejs/biome check --error-on-warnings",
		"storybook": "storybook dev -p 6006 --no-open",
		"storybook-build": "storybook build",
		"test": "echo \"no command specified\"",
		"watch": "vite build --watch src/frontend"
	}
}
